version: '1'

services:
  web:
    containerDefinitions:
      web:
        cpu: 1024 # vCPUs
        memory: 1536 # in MB
        essential: true
        command: "env PORT=3000 bundle exec foreman start web"
        portMappings:
          - containerPort: 3000
